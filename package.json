{
  "name": "leetsync",
  "version": "1.6.0",
  "private": true,
  "description": "Dev tooling for the LeetSync Chrome extension. Not shipped \u2014 package.ps1 has an explicit include list.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "package": "powershell -File package.ps1",
    "sheets": "python scripts/parse-sheets.py",
    "preview": "node scripts/make-preview.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
